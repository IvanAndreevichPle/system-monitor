# Анализ проекта "Системный мониторинг"

## Общее описание
Демон, собирающий системные метрики и отправляющий их клиентам через gRPC (server-side streaming).

## Ключевые требования

### Архитектура
- gRPC сервер с однонаправленным потоком (server-side streaming)
- Статистика хранится в памяти
- Можно использовать временные файлы (/tmp)
- Только стандартная библиотека Go!

### Алгоритм работы
- Каждые **N** секунд выдавать снапшот
- Снапшот усреднен за последние **M** секунд
- Пример: N=5с, M=15с
  - 0-15с: молчит (накапливает данные)
  - 15с: выдает снапшот за 0-15с
  - 20с: выдает снапшот за 5-20с
  - 25с: выдает снапшот за 10-25с

### Что собирать

1. **Load Average** - из /proc/loadavg
2. **CPU** - из /proc/stat (%user, %system, %idle)
3. **Диски** - из /proc/diskstats (tps, KB/s)
4. **Файловые системы** - через df (MB использовано, % использовано, inode)
5. **Top talkers по сети**:
   - По протоколам (TCP, UDP, ICMP) - bytes, %
   - По трафику (source:port -> dest:port) - bps
6. **Сетевые соединения**:
   - Слушающие сокеты (TCP/UDP) - command, pid, user, protocol, port
   - TCP соединения по состояниям (ESTAB, FIN_WAIT, SYN_RCV и т.д.)

### Источники данных

**Только стандартная библиотека Go:**
- `/proc/loadavg` - load average
- `/proc/stat` - CPU статистика
- `/proc/diskstats` - дисковые операции
- `/proc/net/dev` - сетевые интерфейсы
- `/proc/net/sockstat` - статистика сокетов
- `/proc/net/tcp`, `/proc/net/udp` - детали сокетов
- Команды: `df -k`, `df -i`, `ss -ta`, `netstat -lntup`

### Конфигурация
- CLI: порт сервера
- Файл: включение/выключение подсистем сбора

### Тестирование
- Юнит-тесты: моки интерфейсов, вспомогательные функции
- Интеграционные тесты: поток статистики, создание файлов/сокетов
- Простой клиент: вывод статистики в STDOUT (таблица)

## Разбалловка (максимум 20 баллов)

### Базовые метрики (5 баллов):
- Load average - 1 балл
- CPU - 1 балл
- Диски - 1 балл
- Top talkers - 1 балл
- Статистика сети - 1 балл

### Дополнительно:
- Конфигурация отключения подсистем - 2 балла
- Юнит-тесты - 1 балл
- Интеграционные тесты - 2 балла
- Простой клиент - 2 балла
- Поддержка разных ОС - 5 баллов (опционально)
- Чистота кода - до 3 баллов

**Зачёт от 10 баллов**

## Сложность реализации

### Простые части:
- ✅ gRPC сервер (есть опыт)
- ✅ Protobuf (есть опыт)
- ✅ Чтение /proc файлов (стандартная библиотека)

### Сложные части:
- ⚠️ Парсинг /proc файлов (нужно изучить форматы)
- ⚠️ Усреднение за период M секунд (алгоритм скользящего окна)
- ⚠️ Сбор сетевых метрик (требует понимания сетевых протоколов)
- ⚠️ Сбор информации о процессах (PID, user, command)
- ⚠️ Поддержка разных ОС (build tags, разные команды)

## План реализации

1. **Структура проекта**
   - cmd/monitor/ - основной демон
   - cmd/client/ - простой клиент
   - internal/collector/ - сборщики метрик
   - internal/storage/ - хранение метрик в памяти
   - api/proto/ - Protobuf схемы

2. **Этапы разработки**
   - Этап 1: Protobuf схема + gRPC сервер (базовая структура)
   - Этап 2: Сбор Load Average и CPU (простые метрики)
   - Этап 3: Сбор дисковых метрик
   - Этап 4: Сбор сетевых метрик
   - Этап 5: Усреднение за период M секунд
   - Этап 6: Конфигурация
   - Этап 7: Тесты + клиент
   - Этап 8: Dockerfile + CI/CD

